import styled from 'styled-components'

const Button = styled.button`
    background: #62f7db;
    :hover{
        cursor:pointer;    
    }
`
const ButtonDiv = styled.div`
display: flex;
padding: 5px;

`

export {
        Button,
        ButtonDiv,
}